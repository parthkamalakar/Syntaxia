// Auto-extracted from syntaxia.html
export const KB=[
  // Python
  {k:['hello world','print','first program','start python'],lang:'python',
   a:'In Python, use `print()` to display output!\n\n```\nprint("Hello, World!")\n```\n\nThis is the simplest Python program. Every Python program runs line by line from top to bottom. 🐍'},
  {k:['variable','store data','declare'],lang:'python',
   a:'Variables in Python store data. Just write `name = value` — no type needed!\n\n```\nname = "Alex"\nage = 20\nprint(name, age)\n```\n\nPython figures out the type automatically. 🎯'},
  {k:['if','else','condition','elif'],lang:'python',
   a:'Use `if`/`elif`/`else` for decisions. **Indentation matters!**\n\n```\nscore = 85\nif score >= 90:\n    print("A")\nelif score >= 70:\n    print("B")\nelse:\n    print("C")\n```'},
  {k:['loop','for','range','while','iterate'],lang:'python',
   a:'`for` loops repeat code. `range(n)` gives numbers 0 to n-1:\n\n```\nfor i in range(5):\n    print(i)  # 0,1,2,3,4\n```\n\nUse `range(start, stop, step)` for more control! 🔄'},
  {k:['function','def','return'],lang:'python',
   a:'Define reusable functions with `def`:\n\n```\ndef greet(name):\n    return "Hello, " + name + "!"\n\nprint(greet("World"))\n```\n\nFunctions are the building blocks of clean code! 🧱'},
  {k:['list','array','append'],lang:'python',
   a:'Lists store multiple values:\n\n```\nfruits = ["apple", "banana"]\nfruits.append("cherry")\nfor fruit in fruits:\n    print(fruit)\n```\n\nAccess items by index — `fruits[0]` gives `"apple"`. 📝'},
  {k:['dictionary','dict','key','value'],lang:'python',
   a:'Dictionaries store key-value pairs:\n\n```\nperson = {"name": "Alice", "age": 25}\nprint(person["name"])  # Alice\n```\n\nUse `.items()` to loop through both keys and values! 🗝️'},
  {k:['class','object','__init__','self'],lang:'python',
   a:'Classes are blueprints for objects:\n\n```\nclass Dog:\n    def __init__(self, name):\n        self.name = name\n    def bark(self):\n        print(self.name + ": Woof!")\n\nd = Dog("Rex")\nd.bark()\n```'},
  {k:['error','exception','try','except'],lang:'python',
   a:'Handle errors with `try`/`except`:\n\n```\ntry:\n    x = 10 / 0\nexcept ZeroDivisionError:\n    print("Cannot divide by zero!")\nfinally:\n    print("This always runs")\n```'},
  // JavaScript
  {k:['javascript','console.log','js first'],lang:'javascript',
   a:'In JavaScript, `console.log()` prints output:\n\n```\nconsole.log("Hello, World!");\n```\n\nJS runs in browsers AND on servers with Node.js. Every website you visit uses JavaScript! 💛'},
  {k:['const','let','var','javascript variable'],lang:'javascript',
   a:'Use `const` for values that never change, `let` for values that do:\n\n```\nconst name = "Alex"; // cannot reassign\nlet score = 0;       // can reassign\nscore = 100;\n```\n\nAvoid `var` — it has confusing scope rules!'},
  {k:['arrow function','=>','function javascript'],lang:'javascript',
   a:'Arrow functions are the modern way to write functions:\n\n```\nconst square = n => n * n;\nconst add = (a, b) => a + b;\n\nconsole.log(square(4)); // 16\nconsole.log(add(3, 5)); // 8\n```'},
  {k:['array','map','filter','foreach','javascript array'],lang:'javascript',
   a:'JS arrays have powerful methods:\n\n```\nconst nums = [1, 2, 3, 4, 5];\nconst doubled = nums.map(n => n * 2);\nconst evens = nums.filter(n => n % 2 === 0);\nconsole.log(doubled); // [2,4,6,8,10]\nconsole.log(evens);   // [2,4]\n```'},
  {k:['promise','async','await','fetch'],lang:'javascript',
   a:'`async/await` makes async code readable:\n\n```\nasync function getData() {\n  try {\n    const res = await fetch("https://api.example.com/data");\n    const data = await res.json();\n    console.log(data);\n  } catch (e) {\n    console.error(e);\n  }\n}\n```'},
  // HTML
  {k:['html','tag','structure','webpage','first html'],lang:'html',
   a:'HTML structures every webpage:\n\n```\n<html>\n  <head><title>My Page</title></head>\n  <body>\n    <h1>Heading</h1>\n    <p>Paragraph</p>\n  </body>\n</html>\n```\n\nTags wrap content. Most need an opening AND closing tag! 🌐'},
  {k:['link','anchor','href'],lang:'html',
   a:'Create links with the `<a>` tag:\n\n```\n<a href="https://google.com" target="_blank">\n  Visit Google\n</a>\n```\n\n`target="_blank"` opens in a new tab. `href` sets the destination URL. 🔗'},
  {k:['form','input','button','html form'],lang:'html',
   a:'Forms collect user input:\n\n```\n<form>\n  <label for="name">Name:</label>\n  <input type="text" id="name" placeholder="Your name">\n  <button type="submit">Submit</button>\n</form>\n```\n\nAlways pair `<label>` with `<input>` for accessibility! ♿'},
  // CSS
  {k:['css','style','selector','color'],lang:'css',
   a:'CSS styles HTML elements:\n\n```\nh1 {\n  color: #5B6BF8;\n  font-size: 32px;\n  font-weight: bold;\n}\n```\n\nThe selector (`h1`) targets elements. Properties change how they look. 🎨'},
  {k:['flexbox','flex','align','justify'],lang:'css',
   a:'Flexbox centers and aligns elements:\n\n```\n.container {\n  display: flex;\n  justify-content: center;  /* horizontal */\n  align-items: center;      /* vertical */\n  gap: 16px;\n}\n```\n\nThis is the most powerful layout tool in CSS! 💪'},
  {k:['grid','css grid'],lang:'css',
   a:'CSS Grid creates two-dimensional layouts:\n\n```\n.grid {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap: 20px;\n}\n```\n\n`1fr` means "1 fraction of available space". Resize freely! 📐'},
  // SQL
  {k:['sql','select','query','database'],lang:'sql',
   a:'SQL queries databases. `SELECT` retrieves data:\n\n```sql\nSELECT name, email FROM users\nWHERE age > 18\nORDER BY name ASC;\n```\n\n`WHERE` filters rows. `ORDER BY` sorts results. Always end with `;` 🗄️'},
  {k:['join','inner join','left join','sql join'],lang:'sql',
   a:'JOINs combine tables:\n\n```sql\nSELECT orders.id, users.name, orders.total\nFROM orders\nINNER JOIN users ON orders.user_id = users.id;\n```\n\n`INNER JOIN` = only matching rows. `LEFT JOIN` = all left rows + matches. 🔗'},
  {k:['group by','count','sum','avg','aggregate'],lang:'sql',
   a:'Aggregate functions compute values across rows:\n\n```sql\nSELECT department,\n       COUNT(*) AS headcount,\n       AVG(salary) AS avg_salary\nFROM employees\nGROUP BY department\nHAVING AVG(salary) > 70000;\n```'},
  // Java
  {k:['java','system.out','main method','hello java'],lang:'java',
   a:'Every Java program needs a class and `main()` method:\n\n```java\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println("Hello, World!");\n    }\n}\n```\n\nJava is strongly typed — you declare the type of every variable! ☕'},
  {k:['java class','java object','constructor'],lang:'java',
   a:'Classes in Java are blueprints:\n\n```java\nclass Dog {\n    String name;\n    Dog(String name) { this.name = name; }\n    void bark() {\n        System.out.println(name + ": Woof!");\n    }\n}\nnew Dog("Rex").bark();\n```'},
  // General
  {k:['what is programming','learn coding','beginner','start learning','where to begin'],lang:'any',
   a:'Great place to start! 🎉\n\nMy recommendation:\n1. **Python** — easiest syntax, huge community, works for everything\n2. **HTML/CSS** — see results immediately in a browser\n3. **JavaScript** — make websites interactive\n\nPick any course from the left and start with Lesson 1. One lesson at a time! 🚀'},
  {k:['best language','which language','recommend'],lang:'any',
   a:'For beginners: **Python** 🐍\n\nWhy? Simple syntax, no semicolons, reads like English. Used for web, AI, data science, automation.\n\nFor web: **JavaScript** 💛 — runs in every browser on Earth.\n\nFor apps: **Java** ☕ or **Swift** 🦅\n\nStart with Python — you can learn others later! 😊'},
  {k:['hint','stuck','help','how do i','how to','what should i'],lang:'any',
   a:'Happy to help! 💡 Tell me:\n\n1. **Which lesson** you are on\n2. **What you have tried** so far\n3. **What error or issue** you are seeing\n\nI will guide you to the answer without giving it away! 🎯'},
  {k:['debug','error','bug','not working','wrong output'],lang:'any',
   a:'Debugging tips 🔍:\n\n1. **Read the error message** carefully — it tells you the line and type\n2. **Print intermediate values** to see what the code is actually doing\n3. **Check spelling** — variable names are case-sensitive\n4. **Google the exact error** — someone has seen it before!\n\nWhat error are you seeing? 🤔'},
  {k:['recursion','recursive'],lang:'any',
   a:'Recursion is a function that calls itself! 🌀\n\n```python\ndef factorial(n):\n    if n <= 1:      # base case — ALWAYS needed!\n        return 1\n    return n * factorial(n - 1)\n\nprint(factorial(5))  # 120\n```\n\nEvery recursive function needs a **base case** or it loops forever!'},
  {k:['algorithm','big o','time complexity','performance'],lang:'any',
   a:'Big O describes how fast an algorithm grows 📊:\n\n- **O(1)** — constant, same speed always\n- **O(n)** — linear, grows with input size\n- **O(n²)** — quadratic, nested loops\n- **O(log n)** — fast! Binary search\n\nAim for O(1) or O(n) when possible. 💪'},
  {k:['api','rest','endpoint','http'],lang:'any',
   a:'APIs let programs talk to each other! 🔌\n\nREST API basics:\n- **GET** — fetch data\n- **POST** — send/create data\n- **PUT** — update data\n- **DELETE** — remove data\n\nTry `fetch()` in JavaScript to call a public API! 🚀'},
  {k:['git','github','version control','commit','push'],lang:'any',
   a:'Git saves your code history! 🗂️\n\n```bash\ngit init          # start tracking\ngit add .         # stage changes\ngit commit -m "message"  # save snapshot\ngit push          # upload to GitHub\n```\n\nThink of commits like save points in a game — you can always go back! 🎮'},
];
